#include "ARR.h"
#include <iostream>

using namespace std;

int main()
{
	ARR arr;
	build(arr);
	display(arr);

	// -put your custom functions here!------------------
	// --------------------------------------------------

	destroy(arr);

	return 0;
}
